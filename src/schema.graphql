#import './hooks/accounts/Accounts.graphql'
#import './hooks/lastBlock/LastBlock.graphql'

type Config {
    nodeUrl: String!,
    processorUrl: String!
}

extend type Query {
    # just a placeholder to make the codegen not complain about
    # root query not being defined
    _empty: String
}