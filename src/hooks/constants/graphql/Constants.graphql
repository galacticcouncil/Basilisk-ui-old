#import './../../pools/graphql/Pool.graphql'

type RepayFee implements Fee {
  id: String!
  numerator: String!
  denominator: String!
}

type LBPConstants {
  repayFee: RepayFee
}

type Constants {
  lbp: LBPConstants
}

extend type Query {
  constants: Constants!
}
