#import './../../pools/graphql/Pool.graphql'

type LbpConstants {
  id: String!
  repayFee: Fee!
}

type Constants {
  id: String!
  lbp: LbpConstants
}

extend type Query {
  constants: Constants!
}
