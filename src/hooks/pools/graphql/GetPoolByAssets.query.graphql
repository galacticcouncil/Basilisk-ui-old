query GetPoolByAssets($assetAId: String!, $assetBId: String!) {
    pool: pools(assetIds: [$assetAId, $assetBId]) @client {
        id,
        assetAId,
        assetBId,
        balances {
            assetId,
            balance
        }
    }
}