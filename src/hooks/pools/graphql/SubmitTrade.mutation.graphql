mutation SubmitTrade(
    $assetAId: String!, 
    $assetBId: String!,
    $assetAAmount: String!,
    $assetBAmount: String!,
    $poolType: PoolType,
    $tradeType: TradeType
) {
    submitTrade(
        assetAId: $assetAId
        assetBId: $assetBId,
        tradeType: $tradeType,
        assetAAmount: $assetAAmount,
        assetBAmount: $assetBAmount
    ) @client
}