mutation RemoveLiquidity(
    $assetA: String!,
    $assetB: String!,
    $amount: String!
) {
    removeLiquidity(
        assetA: $assetA,
        assetB: $assetB,
        amount: $amount
    ) @client
}