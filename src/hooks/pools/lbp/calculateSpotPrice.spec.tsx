import { HydraDxMath, loadMath } from '../../math/useMath'

describe.only('lbp', () => {
    let math: HydraDxMath;

    beforeAll(async () => {
        // const xykMath = await import('hydra-dx-wasm/build/xyk/bundler');
    });

    describe('calculateSpotPrice', () => {
        it('should calculate a spot price of 1 for equally weighted and liquid reserves', () => {

        })
    })
})