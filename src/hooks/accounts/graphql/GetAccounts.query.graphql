
query GetAccounts {
    lastBlock @client {
        parachainBlockNumber
    }
    
    accounts @client {
        id,
        name,
        isActive,
        balances {
            assetId,
            balance
        }
    }
}