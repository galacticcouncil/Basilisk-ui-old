#import "./AccountFields.fragment.graphql"

query GetActiveAccount {
    lastBlock @client { 
        number 
    }
    
    account: accounts(isActive: true) @client {
        ...AccountFields
    } 
}